# `ios` · 三端 WebView 容器

## 这是什么

三端 WebView 容器的 iOS 那一份。**只是一个把页面装起来跑的原生壳子**，不叫基座、底座或中台。

判据不在本仓：要做什么、怎么算过、有什么还没核实，全在 `pro` 的
`~/Desktop/github/Prospect/plan/` 五份里程碑文件；取值只在那个仓的 README 取值表。
**本仓一条判据也不复述** —— 复述出来就是第二处真相。

按的是 `pro` 的哪个 commit：**`e13f506`**（`加一份开工文档:三端并行的顺序与动作`）。
`pro` 会动（插队 spike 的答案回去、出口 2 / 3 议完都要改文档），
所以**往下一个里程碑走之前重读那六份文件**，重读完把这一行的 commit 更新掉。

## 分工

| 谁 | 出什么 |
| --- | --- |
| `pro` | 契约与规划，写到「哪条约束落在哪个 API」为止；三端共有的取值 |
| 本仓 | 工程形状、目录划分、验收脚本、探针页素材、端侧工程事实、运行记录 |

## 三条纪律，本仓不许自己动

1. **取值一律取 `pro` 的取值表。** 承载 scheme 一类在端内**只定义一处**，别处从它派生，
   不另写字面量。M2 起这条要有一条可执行检查兜着，不是走查
2. **slug、输出形状与探针页那五条规约三端一字不差**，要改三端一起跟
3. **做不到时只能走第 1 个出口（改本端实现）。** 放宽要求、或允许 iOS 偏离，都回 `pro` 议，
   不由本仓自决

## 怎么验证

```bash
./scripts/verify.sh          # 全量：逻辑测试 + 生成工程 + 构建 + 模拟器实跑
./scripts/verify.sh logic    # 只跑包内逻辑测试（秒级，日常改代码用这个）
./scripts/verify.sh app      # 只做工程生成 + 构建 + 模拟器冒烟
./scripts/probe.sh           # 探针页十六条断言（M2 起才有东西跑）
./scripts/probe.sh slugs     # 只打 slug 清单与顺序
```

**改完代码跑 `verify.sh`，不要只靠肉眼判断。** 测试失败就如实报告。
每一步都要能无人工干预地跑完 —— 不点 Xcode、不弹签名对话框、不需要真机；
所以签名全关，验收一律在模拟器上。人工那一遍（M4 按按钮、答对话框、触发后退）
是判据本身要求的，不是验证链条上的例外。

## 目录

```
.
├── CLAUDE.md            # 本文件
├── project.yml          # xcodegen 工程定义（改这个，不改 .xcodeproj）
├── App/                 # 薄 SwiftUI 外壳
│   └── Resources/       # 本地化资源：中文显示名「螃蟹」落在这里
├── scripts/
│   ├── verify.sh        # 验证入口
│   └── probe.sh         # 探针页断言入口，三仓同名
└── docs/
    ├── 工程事实.md       # 工具链门槛与踩坑，重建时从旧仓分流过来的那一份
    ├── M1-任务清单.md    # 每个里程碑一份：把「怎么算过」翻译成本端任务
    └── 运行记录/         # 命令在模拟器上跑出来的输出，对账时贴的就是这些
```

## 约定

- **`.xcodeproj` 是生成物**，不入库。工程结构改 `project.yml`
- **逻辑放 `Packages/`，UI 放 `App/`。** 包内 `swift test` 在宿主 macOS 上秒级返回，
  模拟器构建是分钟级 —— 逻辑写进 App target 就只能靠模拟器验证
- 新增 Swift 文件不用改工程文件（`sources: App` 按目录纳入）。新增 target 才改 `project.yml`
- 文档正文用中文，代码标识符与 slug 用英文
- **运行记录落 `docs/运行记录/`**，形式是脚本在模拟器上跑出来的输出，不重新叙述一遍
- 端侧工程事实（工具链咬合、构建陷阱、环境自检）落 `docs/工程事实.md`，**不上收给 `pro`**；
  平台事实（平台声明、还成立的技术约束）反过来，交给 `pro` 写进对应里程碑的「平台事实」块

## 里程碑进度

| 里程碑 | 状态 |
| --- | --- |
| M1 装到模拟器 | ✅ 过了，记录在 `docs/运行记录/` |
| M1 后的插队核实 | 下一步：承载 scheme 的 `location.origin` 形态、`data:` iframe，答案回 `pro` 再走 M2 |
| M2 本地承载 | 未开工，等 `pro` 把插队答案写进 M2 |
| M3 配置注入与 UA | 未开工 |
| M4 导航与降级 | 未开工 |
| M5 三端汇合 | 未开工 |
